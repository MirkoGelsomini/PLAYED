# Esempio di file .env per avvio locale/produzione
# Copia questo file in .env e personalizza i valori

# Chiave segreta per JWT (obbligatoria, scegli una stringa lunga e casuale!)
JWT_SECRET=metti-qui-una-chiave-segreta

# Stringa di connessione a MongoDB
MONGODB_URI=mongodb://localhost:27017/played

# Porta su cui far partire il backend (opzionale)
PORT=4000